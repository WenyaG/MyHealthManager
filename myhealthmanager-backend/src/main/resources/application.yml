# mysql
spring:
   datasource:
     #MySQL Config
     driverClassName: com.mysql.cj.jdbc.Driver
     url: jdbc:mysql://localhost:3306/myhealthmanager
     username: root
     password: Apple@15eye

mybatis:
   mapper-locations: classpath:mapper/*.xml
   type-aliases-package: com.example.demo.model

server:
   port: 9000
